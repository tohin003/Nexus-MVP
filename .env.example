# NEXUS local demo needs no environment variables or API credentials.
# Never put an AI API secret in VITE_* variables: these are public browser code.
# Future server-only adapter may accept OPENROUTER_API_KEY; not used by this demo.
